import React from 'react'
import ReactDOM from 'react-dom/client'
import UpEventPage from './upEventPage'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <UpEventPage />
  </React.StrictMode>
)
